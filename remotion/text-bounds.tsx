import React, { useLayoutEffect, useRef } from "react";
import { PlayerRef } from "@remotion/player"


export default function TextBounds({ children }: Readonly<{children: React.ReactNode}>) {

    const playerRef = useRef<PlayerRef>(null)

    useLayoutEffect

}